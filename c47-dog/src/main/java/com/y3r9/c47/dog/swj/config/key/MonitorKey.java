package com.y3r9.c47.dog.swj.config.key;

/**
 * The Enum MonitorKey.
 * 
 * @version 1.0
 * @since project 3.0
 */
public enum MonitorKey {

    /** The group. */
    group,

    /** The file set. */
    fileSet,

    /** The file set folder. */
    fileSetFolder,

    /** The file set order. */
    fileSetOrder,

    /** The fileSetBatchSize. */
    fileSetBatchSize,

    /** The action. */
    postAction,

    /** The postActionFolder. */
    postActionFolder,

    /** The monitor scan tick time milliseconds. */
    monitorScanTickMilli,

    /** The parse time milliseconds alert threshold. */
    parseTimeAlertThreshold,
}
