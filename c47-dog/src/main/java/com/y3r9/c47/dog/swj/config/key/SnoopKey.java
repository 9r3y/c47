package com.y3r9.c47.dog.swj.config.key;

// TODO: Auto-generated Javadoc
/**
 * The Enum SnoopKey.
 *
 * @version 1.2
 * @since v3.0
 * @since v3.8 update to 1.1, add timeoutSafeBias.
 */
public enum SnoopKey {

    /** The group. */
    group,

    /** The socket. */
    socket,

    /** The zmq. */
    zmq,

    /** The packet ring. */
    pring,

    /** The interface. */
    intf,

    /** The loader. */
    loader,

    /** The snaplen. */
    snaplen,

    /** The bpf. */
    bpf,

    /** The timeout milliseconds. */
    timeout,

    /** The timeout safe bias milliseconds. */
    timeoutSafeBias,

    /** The device flags. */
    deviceFlags,

    /** The buffer size. */
    bufferSize,

    /** The byte count. */
    byteCount,

    /** The packet count. */
    packetCount,

    /** The cache buffer size. */
    cacheBufferSize,

    /** The break process size. */
    breakProcessSize,

    /** The break process millisecond. */
    breakProcessMilli,

    /** The socket stream. */
    socketStream,

    /** The server address. */
    serverAddress,

    /** The client address. */
    clientAddress,

    /** The device. */
    device,

    /** The slice length. */
    sliceLength,

    /** The endpoint. */
    endpoint,

    /** The high water mark. */
    highWaterMark,

    /** The silent heart beat time milliseconds. */
    silentHeartBeatMilli,

    /** The silent terminate time milliseconds. */
    silentTerminateMilli,

    /** The silent as idle time nanoseconds. */
    silentAsIdleTime,

    /** The device idle sleep time nanoseconds. */
    deviceIdleSleepNano,

    /** The snoop match. */
    snoopMatch,

    /** The snoop id. */
    snoopId,

    /** The autoAssign. */
    autoAssign,

    /** The assignCheckFreqMilli. */
    assignCheckFreqMilli,

    /** The enableUnknownApp. */
    enableUnknownApp,
}
