package com.y3r9.c47.dog.swj.config.key;

/**
 * The Enum MeterKey.
 *
 * @version 1.0
 * @since project 3.0
 */
public enum MeterKey {

    /** The title repeat count. */
    titleRepeatCount,

    /** The global shadow meter scan tick seconds. */
    globalShadowMeterScanTickSec,

    /** The JVM meter scan tick time milliseconds. */
    jvmMeterScanTickMilli,

    /** The cache meter scan tick time milliseconds. */
    cacheMeterScanTickMilli,

    /** The drop meter scan tick time milliseconds. */
    dropMeterScanTickMilli,

    /** The throughput meter scan tick time milliseconds. */
    throughputMeterScanTickMilli,

    /** The throughput meter detail mode. */
    throughputMeterDetailMode,

    /** The snoop meter scan tick time milliseconds. */
    snoopMeterScanTickMilli,

    /** The monitor meter scan tick milliseconds. */
    monitorMeterScanTickMilli,

    /** The ZeroMQ export meter scan tick time milliseconds. */
    zeromqExportMeterScanTickMilli,

    /** The ZeroMQ export decode meter scan tick time milliseconds. */
    zeromqExportDecodeMeterScanTickMilli,

    /** The decode meter scan tick milliseconds. */
    decodeMeterScanTickMilli,

    /** The parallelMeterScanTickMilli. */
    parallelMeterScanTickMilli,

    /** The parallelMeterMonitorSegments. */
    parallelMeterMonitorSegments,
}
