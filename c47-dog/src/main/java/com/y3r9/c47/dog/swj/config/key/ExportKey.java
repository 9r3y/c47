package com.y3r9.c47.dog.swj.config.key;

/**
 * The Enum ExportKey. Should also reference to ExportType for export.
 *
 * @version 1.1
 * @since project 3.0
 */
public enum ExportKey {

    /** The group. */
    group,

    /** The endpoint. */
    endpoint,

    /** The zeromq group. */
    zeromqGroup,

    /** The type. */
    type,

    /** The id. */
    id,

    /** The server. */
    server,

    /** The server address. */
    serverAddress,

    /** The high water mark. */
    highWaterMark,

    /** The header. */
    header,

    /** The pack. */
    pack,

    /** The template. */
    template,

    /** The key value body. */
    body,

    /** The csv body. */
    csvBody,

    /** The title column. */
    titleColumn,

    /** The stdio target. */
    stdioTarget,

    /** The route id. */
    routeId,

    /** The time format. */
    timeFormat,

    /** The export group lookup enable threshold. */
    exportGroupLookupEnableThreshold,

    /** The export sync beat. */
    exportSyncBeat,

    /** The group count. */
    groupCount,

    /** The block size. */
    blockSize,

    /** The rate pps. */
    ratePPS,

    /** The guard on sync message. */
    guardOnSyncMessage,

    /** The guard on time milli. */
    guardOnTimeMilli,

    /** The Instance export key. @since 1.1 */
    instance,
}
