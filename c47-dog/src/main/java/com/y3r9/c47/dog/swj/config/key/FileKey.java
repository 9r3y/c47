package com.y3r9.c47.dog.swj.config.key;

/**
 * The Enum FileKey.
 * 
 * @version 1.3
 * @since v3.0
 * @since v3.1 update to 1.1, add buffer size.
 */
public enum FileKey {

    /** The file. */
    file,

    /** The time file. */
    timeFile,

    /** The simple file. */
    simpleFile,

    /** The name file. */
    nameFile,

    /** The time. */
    time,

    /** The folder. */
    folder,

    /** The name pattern. */
    namePattern,

    /** The file name pattern. */
    fileNamePattern,

    /** The target folder. */
    targetFolder,

    /** The time format. */
    timeFormat,

    /** The time milliseconds span. */
    timeSpan,

    /** The pkt count span. */
    packetCountSpan,

    /** The file size span. */
    fileSizeSpan,

    /** The split by. */
    splitBy,

    /** The group id. */
    groupId,

    /** The buffer size. */
    bufferSize,

    /** The noTempFile. */
    noTempFile,
}
