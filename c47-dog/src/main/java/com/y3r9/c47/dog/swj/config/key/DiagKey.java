package com.y3r9.c47.dog.swj.config.key;

/**
 * The Enum DiagKey.
 *
 * @version 1.0
 * @since project 3.0
 */
public enum DiagKey {

    /** The property key. */
    property,

    /** The resource key. */
    resource,

    /** The nts key. */
    nts,

    /** The nts key. */
    ntr,

    /** The root folder. */
    rootFolder,

    /** The sub folder. */
    subFolder,

    /** */
    startTime,

    /** The duration. */
    duration,

    /** The interval. */
    interval,

    /** The recursion. */
    recursion,

    /** The hasHeader. */
    hasHeader,

    /** The output. */
    output,

    /** The usedMemPercTh. */
    usedMemPercTh,

    /** The gcLoadTh. */
    gcLoadTh,

    /** The delta. */
    delta,

    /** The dpConfFolder. */
    dpConfFolder,

    /** The dpJvmLogFolder. */
    dpJvmLogFolder,

    /** The dpDropLogFolder. */
    dpDropLogFolder,

    /** The dpSnoopLogFolder. */
    dpSnoopLogFolder,

    /** The dpCacheLogFolder. */
    dpCacheLogFolder,

    /** The dpThroughputLogFolder. */
    dpThroughputLogFolder,

    /** The dpZmqLogFolder. */
    dpZmqLogFolder,

    /** The pktminerPerfLogFolder. */
    pktminerPerfLogFolder

}
