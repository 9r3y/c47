package com.y3r9.c47.dog.swj.config.key;

/**
 * The Enumeration PersistenceKey.
 *
 * @version 1.1
 * @since project 3.3
 */
public enum PersistenceKey {

    /** The persistence. */
    persistence,

    /** The key mode. */
    keyMode,

    /** The default key. */
    defaultKey,

    /** The data source. */
    dataSource,

    /** The collections. */
    collections,

    /** The collection. */
    collection,

    /** The URL. */
    url,

    /** The name. */
    name,

    /** The max size. */
    maxSize,

    /** The key size. */
    keySize,

    /** The value size. */
    valueSize,

    /** The remote. */
    remote,

    /** The local. */
    local,

    /** The cache size. */
    cacheSize,

    /** The template. */
    template,

    /** The cleaner. */
    cleaner,

    /** The interval. */
    interval,
}
