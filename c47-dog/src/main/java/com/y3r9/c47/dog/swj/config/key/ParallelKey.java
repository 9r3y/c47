package com.y3r9.c47.dog.swj.config.key;

/**
 * The Enum ParallelKey.
 *
 * @version 1.0
 * @since project 3.0
 */
public enum ParallelKey {

    /** The parallel. */
    parallel,

    /** The worker count. */
    workerCount,

    /** The partition bind. */
    partBind,

    /** The worker to partition factor. */
    workerToPartFactor,

    /** The ways inside group. */
    waysInsideGroup,

    /** The partition count. */
    partitionCount,

    /** The partition to pipe capability factor. */
    partToPipeCapabilityFactor,

    /** The join pipe capability. */
    joinPipeCapability,

    /** The cache token count. */
    cacheTokenCount,

    /** The data type. */
    dataType,

    /** The split node. */
    splitNode,

    /** The partition node. */
    partitionNode,

    /** The work node. */
    workNode,

    /** The join node. */
    joinNode,

    /** The pipe type. */
    pipeType,

    /** The join count. */
    joinCount,

    /** The split count. */
    splitCount,
}
