package com.y3r9.c47.dog.swj.config.key;

/**
 * The enum Match key.
 *
 * @version 1.2
 * @since v3.0
 * @since v3.9 update 1.2
 */
public enum MatchKey {

    /** The stream match. */
    streamMatch,

    /** The stream id. */
    streamId,

    /** The decode match. */
    decodeMatch,

    /** The decode id. */
    decodeId,

    /** The Ethernet type match. */
    etherTypeMatch,

    /** The Ethernet type. */
    etherType,

    /** The value. */
    value,

    /** The IP protocol match. */
    ipProtocolMatch,

    /** The IP protocol. */
    ipProtocol,

    /** The TTL match. */
    ttlMatch,

    /** The TTL. */
    ttl,

    /** The VLAN match. */
    vlanMatch,

    /** The VLAN. */
    vlan,

    /** Mpls match match key. */
    mplsMatch,

    /** Mpls match key. */
    mpls,

    /** The APP match. */
    appMatch,

    /** The APP. */
    app,

    /** The site match. */
    siteMatch,

    /** The site. */
    site,

    /** The spv. */
    spv,

    /** The spv match. */
    spvMatch,

    /** The logical group. */
    logicalGroup,

    /** The logical group match. */
    logicalGroupMatch,

    /** The address match. */
    addressMatch,

    /** The IP port match. */
    ipPortMatch,

    /** The ip1. */
    ip1,

    /** The ip2. */
    ip2,

    /** The port1. */
    port1,

    /** The port2. */
    port2,

    /** The action. */
    action,

    /** The disable IP hash. */
    disableIpHash,

    /** The discard filter. */
    discardFilter,

    /** The Meta type match. @since 1.2 add back. */
    metaTypeMatch,

    /** The Meta type. @since 1.2 add back. */
    metaType,

    /** The agent match. */
    agentMatch,

    /** The agent id. */
    agentId,

    /** The agent sub match. */
    agentSubMatch,

    /** The agent sub id. */
    agentSubId,

    /** The agent source match. */
    agentSourceMatch,

    /** The agent source id. */
    agentSourceId,

    /** The agent intf match. */
    agentIntfMatch,

    /** The intf. */
    agentIntf,
}
